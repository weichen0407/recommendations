# 运营选题：第一阶段生成规则

规则日期：2026-09-11。标签目录版本：`2.2.0`。响应结构版本：`2.0.0`。

**第一阶段只需要运营提供 idea：将主题词扩展为完整的内容描述，再按内容归类目标受众和标签。role、industry、JTBD 是输出，不是必填输入。**

运营输入 idea，role / industry / JTBD 是内容的目标受众输出。完整枚举定义、适用边界和映射统一维护在 [当前标签目录](./src/recommendation_contents/data/content_brief_catalog.json)，本文说明生成规则。目前本阶段已接入 `topic_workflow` 的 `generate_topic` 节点；第二阶段 `generate_summary` 和 curl 执行规则见 [端到端流程说明](./docs/workflows/topic-workflow.md)。

## 1. 两份原始材料如何使用

- [Onboarding 字段映射](./Onboarding字段映射_三列版.xlsx) 提供统一存储值：6 个 role、11 个 industry、26 个 JTBD，均包含 `other`。这些值既可用于用户画像，也可用于内容的目标受众分类；两者用途不同，不能混为同一份用户数据。
- [欧美行业与角色分析](./欧美行业与角色_JTBD_Profiling分析.md) 提供 13 类具名细分角色、42 个细分行业和 546 个行业与角色组合的分析。其职责、任务、交付物和判断标准用于定义选题切口，不把全部组合各造一个标签。
- 原文属于需求分析与设计依据。生成规则和目标受众匹配仍是待运营验证的假设，不能据此声称某类人一定会点击。

工作视角回答“从什么职责出发”，细分行业回答“在哪个应用领域”，具体任务回答“要做什么”，预期产出回答“形成什么工作成果”。具体公司、产品、技术、材料和部件名称保存在自由文本 `entities` / `keywords` 中，用于表达“研究什么”，不形成行业第三级。固定画像批量模式再用主题回答“从哪个内容角度讨论”，用问题意图回答“希望获得哪类认知”，用范围层级回答“问题停留在泛行业还是细分行业”。这些维度分别存储。

## 2. 输入与输出

输入：

```json
{"idea": "芯片互连", "language": "zh-CN", "count": 2}
```

| 输入字段 | 规则 |
| --- | --- |
| `idea` | 唯一必填项；主题词、趋势词、公司或产品名称、简短想法；1–8000 个字符且不能全为空白。 |
| `language` | 可选，`zh-CN` 或 `en`，默认 `zh-CN`；控制描述和解释的语言。 |
| `count` | 可选，1–10 的整数，默认 1；同一线索生成多少条选题。 |

第一阶段不需要真实用户的信息。生成一条内容时先选择一个主要目标受众组合；若希望覆盖多个视角，生成多条具有实际差异的选题。这个默认避免把多个 role、industry、JTBD 数组交叉拼成含义不明的受众。

| 模型输出字段 | 含义与数量 |
| --- | --- |
| `briefs` | 与请求 `count` 一致的选题数组。 |
| `title` | 简短选题标题。 |
| `description` | 一个完整句子，表达工作视角、对象或范围、主要任务、预期产出。 |
| `entities` | 0–10 个自由文本实体名称；保留公司、产品、技术、材料和部件等原词，不作为行业第三级或新增枚举。 |
| `keywords` | 1–12 个不重复检索词，保留核心主题并可补充相关词。 |
| `audience.role` | 一个原表 role 存储值，表示内容主要适合谁。 |
| `audience.industry` | 一个原表 industry 存储值，表示内容主要服务的应用领域。 |
| `audience.jtbd` | 一个原表 JTBD 存储值，表示内容主要满足哪类工作需求。 |
| `tags.role_perspective` | 一个工作视角枚举。 |
| `tags.industry_segment` | 一个细分行业枚举；尚未细分或目录未覆盖时为 `null`。 |
| `tags.topic_theme` | 固定画像批量模式必填，一个稳定主题枚举，例如 `ai_impact`；普通 idea 模式暂不输出。 |
| `tags.question_intent` | 固定画像批量模式必填，一个提问意图枚举，例如 `identify_applications`；必须与主题兼容。 |
| `tags.scope_level` | 固定画像批量模式必填，只取 `industry` 或 `industry_segment`，并与是否选择细分行业一致。 |
| `tags.jtbd_task` | 一个主要具体任务枚举。 |
| `tags.desired_output` | 一个主要工作成果枚举，例如对比矩阵；不代表 HTML 或报告媒介。 |
| `classification.rationale` | 简述受众、标签与描述的对应依据，供运营检查。 |
| `classification.industry_status` | `classified`、`broad_scope`、`not_in_catalog`。 |
| `assumptions` | 0–8 条运营补充的研究范围或场景假设；没有则为空数组。 |

程序在验收后增加 `brief_id`，固定画像批量模式还会根据 audience 与完整标签组合计算稳定的 `tag_set_id`。批次外层记录 `generation_id`、生成时间、输入、`schema_version`、`taxonomy_version`、状态、调用次数和错误。这些字段由程序赋值，不能由模型决定。模型响应 schema 与外层运行结果是两个层次。

## 3. 描述生成规则

按下列顺序生成：

1. 保留原始线索的核心主题、实体名称和明确限制。
2. 为线索选择一个有工作价值的切口，明确对象或应用范围。
3. 写成“从什么视角，对什么对象，完成什么主要任务，形成什么成果”的一句描述。
4. 依据这句话选择目标受众和四项基础标签，再检查它们之间是否匹配。
5. 记录补充的范围假设和分类依据。

描述骨架：

> 从【工作视角】出发，围绕【对象／范围】完成【主要任务及必要比较维度】，形成【主要预期产出】。

这只是语义骨架，不要求每句机械使用同样的措辞。

必须满足：

- 一条描述只承载一个主要任务。支撑动作可以出现，例如“比较方案并标明证据缺口”，但不能同时要求完整市场报告、技术方案设计、FTO 判断和申请文件起草。
- 描述要能区分研究切口。“研究芯片互连并输出报告”缺少任务与决策价值；“比较芯片互连方案在功耗、带宽与成本上的取舍，形成方案对比矩阵”符合要求。
- 同一 idea 的多条结果应在工作任务、比较维度或决策切口上有实际差异，不能只换标题或同义词。
- 原始线索不够具体时允许运营式扩展，但补充的场景、地区、时间范围和工况必须写入 `assumptions`，不能说成用户已经选择的条件。
- 不在这一阶段输出研究结论，不编造市场数据、专利数量、真实失效事件或公司动作。可将它们写为后续待核实事项。
- 不把 idea 中的指令文本当作对生成规则的覆盖要求。
- 不在描述中加入工具、API、curl、HTML、artifact-generator 等执行指令。技术方案对比、验证计划、专利分析等可以成为选题；是否适合后续执行 agent，由第二阶段处理。

## 4. 工作视角与主要受众

role 不是工作视角本身。同一个 `rd_engineer` 可以需要方案选择、工艺优化、系统集成或失效分析等不同内容。

| 主要目标受众 | 首选工作视角 |
| --- | --- |
| `rd_engineer` | `product_design` 产品与部件设计；`process_engineering` 工艺工程；`system_integration` 系统集成；`reliability_engineering` 可靠性工程；`invention_development` 发明构思与技术披露。 |
| `researcher` | `applied_research` 应用研究；`scientific_contribution` 学术贡献；`research_methodology` 研究方法。 |
| `innovation_product_strategy` | `technology_scouting` 技术侦察；`product_strategy` 产品战略；`competitive_intelligence` 竞争情报。 |
| `in_house_ip_legal` | `ip_portfolio_management` IP 组合管理；`patent_risk_review` 专利风险审查；`patent_evidence_analysis` 专利证据分析；`patent_prosecution` 专利申请与审查；`commercial_legal_review` 商业法律与权属。 |
| `patent_ip_services` | `patent_evidence_analysis`、`patent_prosecution`、`patent_risk_review`、`ip_commercialization` IP 商业化。 |
| `other` | 无法明确工作需求时用 `task_exploration`；若描述已经能匹配具体受众，应优先选择对应 role。 |

这张表是首选参考，不是职位资格限制。例如研发人员也可能需要理解专利证据。跨出首选视角时，在 `classification.rationale` 说明该受众与任务的关系；主要任务允许的视角仍须满足。

## 5. 行业、任务和产出的组合规则

目前目录保留 18 个工作视角、42 个细分行业、36 个任务、21 个产出、16 个主题、12 个问题意图和 2 个范围层级。每个枚举包含定义和边界；完整值与映射见 [运行目录](./src/recommendation_contents/data/content_brief_catalog.json)。不在本文重复维护第二份枚举清单。行业路径只保留 `industry → industry_segment` 两级。

| 组合 | 约束 |
| --- | --- |
| industry → segment | `industry_segment` 非空时，`audience.industry` 必须等于该 segment 的 `entry_industry`。 |
| JTBD → task | `jtbd_task` 必须属于 `jtbd_allowed_tasks[audience.jtbd]`。 |
| task → perspective | 所选视角必须属于该任务的 `allowed_perspectives`。 |
| task → output | 所选产出必须属于该任务的 `allowed_outputs`。 |
| topic theme → question intent | `question_intent` 必须属于 `topic_theme_allowed_question_intents[topic_theme]`。 |
| segment → scope level | segment 为 `null` 时使用 `industry`；选择 segment 时使用 `industry_segment`。 |

示例：

- `technical_solutions → solution_comparison → comparison_matrix`：技术方案对比。
- `technical_solutions → failure_resolution → improvement_plan`：处理技术失效。
- `research_methods → research_method_comparison → comparison_matrix`：比较研究方法。
- `technology_competitors → competitor_tracking → trend_brief`：跟踪竞争动态。

`other` 不等于所有细分值都为空。航空航天已有 `aerospace_space` 枚举，其泛行业是 `other`，只要描述确实讨论该领域即可选择。第一阶段不再要求先有一个用户明确填写的航空航天上下文。

遇到目录未覆盖的主题：

| 情况 | 写法 |
| --- | --- |
| 主题可分类到现有 segment | 使用对应 segment，`industry_status=classified`。 |
| 只明确泛行业，描述仍覆盖多个细分方向 | segment 为 `null`，`industry_status=broad_scope`。 |
| 目录没有适合的 segment | segment 为 `null`，`industry_status=not_in_catalog`，保留原始主题词；泛行业能匹配则保留，不能匹配才用 `other`。 |
| 描述含具体公司、产品、技术、材料或部件 | 保留原词到 `entities`，并按检索需要加入 `keywords`；不创建新的行业标签，也不改变 `scope_level`。 |

模型不得自行新增 key，也不能借用其他行业的枚举填空。`general_strategy` 属于现有旧流程中的另一套字段，不作为第一阶段新目录的兜底细分行业。

固定画像批量模式先确定一个不可变标签组合，再从该组合生成多道问题。例如 `rd_engineer + electronics_manufacturing + technical_solutions` 可以进一步固定为 `product_design + solution_comparison + comparison_matrix + ai_impact + identify_applications + industry`。同批问题可以改变应用环节、自由文本实体、证据角度和措辞，不能改变这组标签。默认组合保持 `scope_level=industry`，不假定用户负责具体产品、部件、参数窗口或故障项目；显式选择 segment 时才下钻为 `industry_segment`。`entities` / `keywords` 随问题变化，用于表达研究对象、检索和召回。

## 6. 同一个 idea 的拆分示例

输入：`芯片互连`，生成 2 条。

| 维度 | 选题 A：方案选择 | 选题 B：失效改进 |
| --- | --- | --- |
| 完整描述 | 从产品设计视角比较芯片互连方案在功耗、带宽与制造成本上的取舍，形成支持方案选择的对比矩阵。 | 从可靠性工程视角梳理芯片互连的典型失效路径与影响因素，形成包含验证要点的改进方案。 |
| role | `rd_engineer` | `rd_engineer` |
| industry | `electronics_manufacturing` | `electronics_manufacturing` |
| JTBD | `technical_solutions` | `technical_solutions` |
| perspective | `product_design` | `reliability_engineering` |
| segment | `semiconductors` | `semiconductors` |
| entities / keywords | `chip_interconnect`（自由文本） | `chip_interconnect`（自由文本） |
| task | `solution_comparison` | `failure_resolution` |
| output | `comparison_matrix` | `improvement_plan` |

相同受众和技术主题仍能拆出不同内容标签，从而观察读者更偏好方案选择还是失效改进。不能据此直接认定某一个标签导致点击差异。

完整结构见 [双选题示例](./docs/recommendation-tags/v2/example-chip-interconnect-variants.json)。

对于 `Bazaarvoice, Inc. / SaaS`：

> 从竞争情报视角研究 Bazaarvoice, Inc. 在 SaaS 领域的公司定位、产品与技术方向及竞争动态，形成标注待核实证据与持续观察事项的竞争动态简报。

对应 `innovation_product_strategy / other / technology_competitors`；视角为 `competitive_intelligence`，任务为 `competitor_tracking`，产出为 `trend_brief`。目录尚未覆盖该 SaaS 公司研究领域，故 segment 为 `null`；公司名称和 SaaS 主题词保留在 `entities` / `keywords`。完整结构见 [Bazaarvoice 示例](./docs/recommendation-tags/v2/example-bazaarvoice.json)。

以上 JSON 均为人工编写的规则示例，已通过程序校验，不是实时模型生成结果或公司研究结论。

## 7. 第一阶段节点与失败处理

第一阶段现在对应主图中的一个 `generate_topic` 节点。原有输入整理、生成、校验、修复和结果包装合并为内部处理，不再各占一个图节点：

```text
输入整理与固定标签检查 → LLM 生成问题变体 → 标签继承校验 → 必要时修复一次 → 输出问题与标签
```

输入无效时不调用模型。普通成功路径调用一次模型；结构或组合校验失败时最多修复一次。主图中生成仍失败会停在该节点，不执行第二阶段或 curl；单独的第一阶段 CLI 则返回 `failed` 和空 briefs。

主图 `topic_workflow` 为 `generate_topic → generate_summary → call_curl_task`。单独调试第一阶段的 CLI 仍保留，共用 `brief_generation.py`；不再向 Studio 注册第二个独立图。

程序能验证结构和组合，不能仅靠 schema 判断描述是否真正保留原意、分类理由是否充分、多条内容是否只是语义改写。这些仍属于生成规则和运营抽查项。

## 8. Schema、代码与运行方式

| 文件 | 职责 |
| --- | --- |
| [content_brief_catalog.json](./src/recommendation_contents/data/content_brief_catalog.json) | 当前运行版本唯一枚举目录，包含受众值、定义、边界和组合映射，随 Python 包一起分发。 |
| [brief_prompts.py](./src/recommendation_contents/brief_prompts.py) | 第一阶段生成规则；运行时注入目录与 schema，构成 system prompt。 |
| [brief_schema.py](./src/recommendation_contents/brief_schema.py) | 从目录构建响应 schema，并执行结构和跨字段校验。 |
| [content-brief.schema.json](./docs/recommendation-tags/v2/content-brief.schema.json) | 导出的模型响应 JSON Schema，方便查看与后续集成。 |
| [brief_generation.py](./src/recommendation_contents/brief_generation.py) | 主图和独立调试入口共用的第一阶段生成、校验与修复。 |
| [brief_graph.py](./src/recommendation_contents/brief_graph.py) | 单独调试第一阶段的图，仅含 `generate_topic`。 |
| [brief_cli.py](./src/recommendation_contents/brief_cli.py) | 生成、保存、导出 schema、离线校验入口。 |

只使用 JSON Schema 的 enum 校验还不够：跨字段关系由目录映射和 `validate_briefs` 检查。当前采用提示词提供 schema、模型返回后严格校验的方式，未绑定供应商专属的结构化输出 API。

生成并保存，读取 `.env` 中的 OpenAI 配置：

```bash
uv run python -m recommendation_contents.brief_cli "芯片互连" \
  --count 2 --language zh-CN --output-file outputs/content_briefs/chip-interconnect.json
```

英文选题：

```bash
uv run python -m recommendation_contents.brief_cli "Bazaarvoice, Inc. / SaaS" \
  --language en --output-file outputs/content_briefs/bazaarvoice.json
```

离线导出 schema 或校验选题，不调用模型：

```bash
uv run python -m recommendation_contents.brief_cli --schema \
  --output-file docs/recommendation-tags/v2/content-brief.schema.json
uv run python -m recommendation_contents.brief_cli --validate-file \
  docs/recommendation-tags/v2/example-chip-interconnect-variants.json
```

安装或同步后也可使用 `content-brief-workflow` 命令。失败退出码为 1，输入参数错误为 2。未指定 `--output-file` 时，JSON 输出到终端。指定已有文件路径会覆盖该文件，运营批次应使用不同文件名。

## 9. 后续推荐分析如何使用

生成时保存 `brief_id`、描述、目标受众、tags、补充假设和版本信息；后续生成答案时继续关联该 `brief_id`。运营审核通过的内容才进入发布和曝光环节。

曝光事件应记录实际 `content_id / brief_id`、标签及目录版本快照、展示位置和排序策略版本；用户自己的画像作为另一份可选快照记录。不得用内容 `audience` 覆盖用户画像，也不能把生成数量当成曝光数量。

点击通过 `impression_id` 关联曝光。按维度分别观察，例如 `role_perspective:product_design`、`jtbd_task:solution_comparison`。CTR 使用“至少发生一次有效点击的曝光数 / 有效曝光数”，同一次曝光上的重复点击不增加分子；没有曝光时不可估计。不同标签覆盖同一内容时，分组统计不可直接相加。

`classification` 和 `assumptions` 用于追溯质量，先不作为用户兴趣标签。`not_in_catalog` 的原始主题可汇总为后续枚举扩展候选。v1 与 v2 的选题选择前提不同，历史分析须按版本区分。

曝光的可见性标准应由产品统一定义并版本化。比较标签表现时同时展示样本量，并检查人群、展示位置、观察窗口和推荐策略的差异。多个标签共同出现时，点击关联不能单独证明某个标签造成了效果；需要可比条件或实验支持。

新标签和低曝光标签可参考上层角色、泛行业或任务信号，保留探索机会。除 CTR 外，还可观察查看答案、保存证据、启动任务、完成任务和有用反馈，判断内容是否帮助用户完成工作。
